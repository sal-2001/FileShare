<!DOCTYPE html>
<html lang="en">
    <head >
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE-edge">
        <meta name="viewport" content="width-device-width, initial-scale=1.0">
         <title>FileShare</title>
    </head>
    <body>
        <div style="display: flex; flex-direction: column; 
        justify-content: center; padding: 1rem; background-color: white;
        min-height: 100vh;">
        <% if (locals.error) {%>
            <div style="display: flex; justify-content: center; margin-bottom:1rem; color: red;">
               Incorrect Password!    
            </div>
        <%}%>
        <form method="post" style="display: grid; gap: .5rem; grid-template-columns: auto 1fr;
        max-width: 500px; margin: 0 auto">
        <div style="display: flex; flex-direction: column; 
        justify-content: center; padding: 1rem; background-color: rgb(11, 202, 173);
        border: solid; border-width: 2px; border-color: black;">
            <label style="color: black; font-size: 18px; font-weight: 600;" for="password">Password:</label>
            <input type="password" id="password" name="password" required/>
            <button style="color: white; background-color: black; width: 100%; cursor: pointer;
            height: 30px; border-radius: 8px; margin-top: 10px;"type="Submit">Download</button>
        </div>    
        </form>
        </div>
    </body>
</html>